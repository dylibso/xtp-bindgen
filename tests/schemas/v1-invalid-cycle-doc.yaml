# Note: this normally points to https://xtp.dylibso.com/assets/wasm/schema.json
# yaml-language-server: $schema=../../schema.json
---
version: v1-draft
exports:
  export1:
    description: This is an export
    input:
      contentType: application/json
      type: string
    output:
      contentType: application/json
      "$ref": "#/components/schemas/ComplexObject"
components:
  schemas:
    ComplexObject:
      type: object
      description: A complex json object
      properties:
        cycle:
          "$ref": "#/components/schemas/AnotherType"
    AnotherType:
      type: object
      description: Another type
      properties:
        complexObject:
          "$ref": "#/components/schemas/ComplexObject"

